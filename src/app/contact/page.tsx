import { ContactContent } from "./ContactContent";

export const metadata = {
  title: "CONTACT_NODE — Establish Connection",
  description: "Initiate a direct link for collaboration, technical inquiries, or strategic partnerships.",
};

export default function ContactPage() {
  return <ContactContent />;
}
